#include <stdio.h>
#include <stdlib.h>
#define SIZE 100

int g_i = 1, g_j = 2, g_k, g_h;
/*
int main()
{
	const int c_i = 1, c_j = 2;
	int * p = (int *)malloc(sizeof(int) * SIZE);

	int i = 1, j = 2, k, h;
	
	static int s_i = 1, s_j = 2, s_k, s_h;

	char * p1 = "Hello world";
	char * p2 = "MoreWindows";

	printf("堆中数据地址（有初始值）0x%p = %d\n", p, *p);

	printf("栈中数据地址（有初始值）0x%08x = %d\n", &i, i);
	printf("栈中数据地址（有初始值）0x%08x = %d\n", &j, j);

	printf("全局中数据地址（有初始值）0x%08x = %d\n", &g_i, g_i);
	printf("全局中数据地址（有初始值）0x%08x = %d\n", &g_j, g_j);
	printf("全局中数据地址（无初始值）0x%08x = %d\n", &g_k, g_k);
	printf("全局中数据地址（无初始值）0x%08x = %d\n", &g_h, g_h);

	printf("静态中数据地址（有初始值）0x%08x = %d\n", &s_i, s_i);
	printf("静态中数据地址（有初始值）0x%08x = %d\n", &s_j, s_j);
	printf("静态中数据地址（无初始值）0x%08x = %d\n", &s_k, s_k);
	printf("静态中数据地址（无初始值）0x%08x = %d\n", &s_h, s_h);

	printf("数常中数据地址（有初始值）0x%08x = %d\n", &c_i, c_i);
	printf("数常中数据地址（有初始值）0x%08x = %d\n", &c_j, c_j);
	printf("字常中数据地址（无初始值）0x%08x = %s\n", p1, p1);
	printf("字常中数据地址（无初始值）0x%08x = %s\n", p2, p2);

	system("pause");
	return 0;
}*/